model_c_obj(models)::=<<
 <models:{m| <m.name> <\n> <model_schema(m)>};separator="\n\n\n">
>>

model_schema(model)::=<<
<if(model.streaming || model.local)> Model if streaming or local, dont care; <elseif(model.replicated)> Model is so local yum yum<endif>
typedef struct
{
<model.schema:{s| <s.type> \\
<s.name> //<s.comment> ;};separator="\n">
} <model.name>;
>>

