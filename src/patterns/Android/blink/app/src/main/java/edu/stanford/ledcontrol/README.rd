Ravel controller is run as a separate process.
It' responsibilities are to:
start and manages BLE connections
start and mange Cloud connections

create models

on received data update model 
notify 

Models are singelton's
