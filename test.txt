streaming model Random(size):
    properties:
        durable=false
        reliable=false
        records = size

    schema:
        time=TimeStampField(auto_add=True)
        value = IntegerField()
