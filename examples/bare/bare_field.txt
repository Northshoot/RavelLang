struct Accelerometer:
    x : number
    y : number
    z : number

field  AccelerometerField: Accelerometer

    def is_valid(self):
        return True


streaming model AccelerometerModel(size):
    properties:
        durable=False
        reliable=False
        records = size

    schema:
        time=TimeStampField()
        value = IntegerField()